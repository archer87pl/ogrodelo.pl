import { CALCULATORS } from "./calculators";
import { FLOWERING_PLANTS_LIST } from "./flowering-plants";

/** Liczba narzędzi ogrodowych na stronie (do meta opisów). */
export const TOOL_COUNT = CALCULATORS.length;

/** Liczba gatunków w katalogu kwitnienia. */
export const FLOWERING_PLANT_COUNT = FLOWERING_PLANTS_LIST.length;
